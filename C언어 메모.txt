_CRT_SECURE_NO_WARNINGS

C언어에 넣는거



// 저장(ctrl+S) -> 빌드 -> 실행(디버그하지않고시작)

// Q1. cmd창(콘솔, .exe실행파일)이 안나와요.
// A1. 프로젝트-속성-링커-시스템-하위시스템:콘솔

// Q2. main이(가) 이미 정의되어 있습니다.
// A2. 빌드에서 제외해보세요!

// Q3. scanf에서 오류발생했어요
// A3. 프로젝트-속성-C/C++-전처리기-전처리기 정의 변경하기!!!(_CRT_SECURE_NO_WARNINGS)

// Q4. 쓰기용으로 열수없습니다...
// A4. 다른 cmd창(.exe 실행파일)이 동작중 -> 닫아주시고, 다시 실행해주세요

// Q5. 빌드에서 제외
// A5. 해당 소스코드 -> 우클릭 -> 속성 -> 일반 -> 빌드에서 제외: 예